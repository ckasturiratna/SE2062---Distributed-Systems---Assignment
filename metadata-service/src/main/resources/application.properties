server.port=${SERVER_PORT:8081}

# Ratis configuration
ratis.node.id=${RATIS_NODE_ID:metadata-1}
ratis.nodes=${RATIS_NODES:metadata-1:localhost:9001}
ratis.port=${RATIS_PORT:9001}
ratis.data.dir=${RATIS_DATA_DIR:./data/ratis}

# Logging
logging.level.com.distributed.filestorage=DEBUG
logging.level.org.apache.ratis=INFO